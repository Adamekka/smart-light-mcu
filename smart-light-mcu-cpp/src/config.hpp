// Define the pins
// Replace the pins with your own
// Using ESP32 Dev Module (ESP32-WROOM-32) pins
// Make sure you set `PIR_PIN` to an interrupt pin
#define LED_PIN 13
#define PIR_PIN 19

// Define delay
// Replace the delay with your own
// Delay is in milliseconds
// Time to wait before turning the light off
#define DELAY 500

// Define key for IFTTT
// Replace the key with your own
// Key is used to trigger the IFTTT event
#define IFTTT_KEY "5DkTwLg-XvcT5TIdFTF0N"
